package jp.kobespiral.sandazerocarbonappbackend.domain.entity;

/**
 * ミッションタイプ Enumの定義
 *
 * @author Marin checked ing
 */
public enum MissionType {
    DoType, // 実行タイプ(時間で計れない)
    TimeType // 時間タイプ(時間で計れるもの)
}
